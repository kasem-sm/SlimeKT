apply plugin: 'com.android.library'
apply plugin: 'dagger.hilt.android.plugin'

apply {
	from("$rootDir/gradle/non-compose.gradle")
}

apply plugin: 'kotlin-kapt'

provideAndroidXWorkerDeps(this)
provideHiltDeps(this)

dependencies {
	implementation(project(":core"))
	implementation(project(":task-api"))
	implementation implementation(project(':features:authentication:auth-verify-worker',))
	implementation implementation(project(':features:topic:subscription-manager-worker',))

	// Worker
	implementation implementation(project(':features:article:daily-read-worker',))
}
