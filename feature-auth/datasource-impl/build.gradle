plugins {
	id("java-library")
	id("org.jetbrains.kotlin.jvm")
}

java {
	sourceCompatibility = JavaVersion.VERSION_1_8
	targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
	implementation(project(":core"))
	implementation(project(":feature-auth:datasource"))

	// Ktor (with)
	implementation(Jetbrains.ktorJvm)
	implementation(Jetbrains.ktorUtils)
	implementation(Jetbrains.ktorClientCore)

	// Inject
	implementation(JavaX.inject)

	// Hilt
	implementation("com.google.dagger:hilt-core:2.40.5")
}